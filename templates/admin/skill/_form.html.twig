{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

    {{ form_label(form.titleEn) }} {{ form_widget(form.titleEn) }}{{ form_errors(form.titleEn) }}
   {{ form_label(form.titleFr) }} {{ form_widget(form.titleFr) }}{{ form_errors(form.titleFr) }}
   {{ form_label(form.descriptionEn) }}{{ form_widget(form.descriptionEn) }}{{ form_errors(form.descriptionEn) }}
   {{ form_label(form.descriptionFr) }}{{ form_widget(form.descriptionFr) }}{{ form_errors(form.descriptionFr) }}
   {{ form_label(form.skilltype) }} {{ form_widget(form.skilltype) }}{{ form_errors(form.skilltype) }}
<b>{{ form_label(form.levels) }}</b> {{ form_widget(form.levels) }}

<ul id="levels"
    data-prototype="{{ form_widget(form.levels.vars.prototype)|e }}"
    data-widget-levels="{{ '<li></li>'|e }}">
     
</ul>
<!--<a href="#" id="add_level" class="btn btn-default">Add Level</a>-->
<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}


<script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>

<script type="text/javascript">
    $(document).ready(function () {
        var $container = $('div#skill_levels');

        var index = $container.find(':input').length;


        if (index == 0) {
            for (var i = 1; i <= 5; i++) {
                addLevel($container);
            }
        }

        function addLevel($container) {


            var template = $container.attr('data-prototype')
                .replace(/__name__label__/g, 'Level ' + (index + 1))
                .replace(/__name__/g, index)
            ;

            var $prototype = $(template);


            $container.append($prototype);

            index++;
        }


    });
</script>