{% extends 'base.html.twig' %}

{% block title %}Edit Family | {{ parent() }}{% endblock %}

{% form_theme form _self %}
{% block _family_disciplines_entry_row %}
    <div class="item main-item">
        <div class="item-inner">
            <div class="row">
                <a href="{{ path('discipline_edit', {'id': form.vars.value.id}) }}" class="col-6 title">{{ form.vars.value }}</a>
            </div>
        </div>
    </div>
{% endblock %}

{% block body %}
<section class="container mt-5">
        <h1>Job family</h1>
    </section>
    <section class="container mt-5 mb-5">
        <h3 class="text-darkgrey">Edit</h3>
        {{ include('admin/family/_form.html.twig', {'button_label': 'Update'}) }}
    </section>
{% endblock %}
